# 儿童成长积分助手 · 项目开发需求说明书
## （一年级场景 · 微信小程序优先，保留多端 / App 扩展）

**文档版本**: v1.2
**创建日期**: 2026-09-13（v1.2 更新：Q1 后端由「微信云开发」修订为「uniCloud」，打通多端）
**文档定位**: 面向「一年级小朋友 + 家人」的每日任务积分与习惯养成产品，微信小程序优先落地，架构保留多端
**状态**: 已确认关键决策，可进入开发

---

## 0. 文档目的与阅读指南

本文在仓库现有 6 份文档基础上重新梳理需求：

| 现有文档 | 定位 | 本文与其关系 |
|---------|------|------------|
| README.md / OVERVIEW.md | 项目总览 | 继承产品愿景 |
| PRD.md | 通用平台 PRD（4–14 岁、10 模块、含变现） | 本文 = 其 **MVP 子集 + 场景特化** |
| TECHNICAL_SPEC.md | 技术实现（uni-app + 云开发、11 表、30+ API） | 本文取其 **最小可用集** 并给出选型建议 |
| GITHUB_RESEARCH.md | 竞品调研 | 本文引用其结论，标注借鉴点 |
| PROJECT_PLAN.md | 14 周计划 | 本文重排为 **里程碑制**（MVP 优先） |

> **核心变化**：现有 PRD 是「大而全的通用家庭积分平台」，本文收敛为「**一年级每日任务 · 按时完成 · 积分激励 · 习惯养成**」这一条主线。游戏化（宠物/成就）、AI 记账、商业化等**一律后置**，避免第一版失焦（多端能力按 Q1 决策保留）。

---

### 0.1 已确认决策（2026-09-13）

| # | 决策项 | 结论 |
|---|--------|------|
| Q1 | 技术架构 | **uni-app (Vue 3) + uniCloud**，保留多端，为未来手机 App 预留 |
| Q2 | 孩子端形态 | **家长手机上的展示视图**（不做孩子独立账号） |
| Q3 | 打卡与审核 | **在家长手机上打卡，无需审核、自动通过** |
| Q4 | 积分周期 | **不清零**；支持兑换，兑换后扣减 |
| Q7 | 目标用户 | **孩子的家人**：父母、爷爷奶奶、外公外婆等 |
| Q5 | 「按时」宽限期 | 暂定默认 **10 分钟**（后续可调） |
| Q6 | 首个里程碑 | 建议**先做 M1** 跑通闭环（待最终确认） |

---

## 1. 需求分析

### 1.1 真实使用场景澄清（关键）

一年级孩子（6–7 岁）的现实约束会直接决定产品形态，必须首先澄清：

- ❗ **一年级孩子基本无法独立持有微信 / 手机**，因此「孩子独立登录小程序」在 MVP 阶段不成立。
- ✅ 真实链路是：**家长用自己微信登录并操作**，孩子通过「家长递手机看今日任务 / 一起打卡 / 看积分和宠物」的方式参与。
- ✅ 因此 MVP 的产品形态应是 **「家长端为主 + 孩子展示视图」**（同一小程序内切换角色视角），而非「双端双账号」。

> 这一点与现有 PRD「孩子独立登录、双角色账号」的假设不同，是本轮需求最重要的调整。

### 1.2 核心需求（做对这四件事即成功）

| # | 核心需求 | 说明 |
|---|---------|------|
| 1 | **每日规定任务清晰可见** | 作业、跳绳、写字、阅读等固定任务，一眼看清「今天要做什么、几点前做完」 |
| 2 | **打卡极简 + 按时判定** | 孩子/家长 3 步内完成打卡；系统自动判定是否「按时完成」 |
| 3 | **积分即时、透明、可解释** | 完成即得积分、变化有明确原因，孩子能建立「做到 → 得到」的因果认知 |
| 4 | **长期可坚持、可看见进步** | 连续打卡、成长日历/趋势，让孩子和家长都看得到「坚持」的价值 |

### 1.3 用户痛点

- 传统口头催促枯燥，孩子缺乏内在动力；
- 奖惩随意、缺乏一致规则，家长自己也难坚持；
- 习惯记录碎片化（今天记了明天忘），看不到长期变化；
- 一年级孩子识字少，花哨/文字多的产品用不起来。

### 1.4 一年级场景的关键设计约束

| 约束 | 对设计的影响 |
|------|------------|
| 识字量有限 | 界面以 **图标 + 颜色 + 语音/动画** 为主，文字尽量短，避免长句 |
| 注意力短 | 首页信息极简；打卡反馈要 **即时、夸张、有仪式感**（音效/动画/贴纸） |
| 由家长代操作 | 家长操作路径要 **≤3 步**；家长端功能要能快速完成（碎片时间可操作） |
| 任务高度固定 | 一年级每日任务高度重复 → **任务模板库** 是刚需，避免每天重复创建 |
| 家长也需坚持 | 产品要帮家长「省事」：批量打卡、一键补录、提醒 |
| 合规敏感 | 儿童信息采集最小化，无需人脸/定位/声纹 |

### 1.5 与现有文档的差异（Scope 收敛）

| 维度 | 现有 PRD | 本文（一年级 MVP） |
|------|---------|------------------|
| 目标年龄 | 4–14 岁 | **6–7 岁（一年级）** |
| 角色模型 | 家长 / 孩子双账号 | **家长账号 + 孩子展示视图** |
| 任务 | 日常/一次/挑战/习惯 四类 | **每日固定任务为主**（一次性任务次之） |
| 积分 | 含随机奖励、生日/周末倍数等 | **按时判定 + 基础分 + 连续奖励**，去掉随机分（保证可解释） |
| 游戏化 | 宠物 + 成就（一期） | **二期**再引入 |
| AI 记账 | 一期规划 | **后置**（非核心） |
| 变现 | 免费/付费/年费 | **不做**（家庭自用/小范围） |
| 多端 | 小程序 + H5 + 后台 | **微信小程序优先**，架构保留多端 / App 扩展 |
| 周期 | 14 周全功能 | **MVP 约 2–3 周可用，完整 V1 约 6–8 周** |

---

## 2. 产品目标与成功指标

### 2.1 目标

- **对孩子**：让「按时完成每日任务」变得有即时反馈、有成就感。
- **对家人**：把「催孩子」变成「一起看进度」，父母、爷爷奶奶、外公外婆都能轻松参与，管理负担降到最低。
- **对家庭**：形成稳定、可长期坚持的每日习惯闭环。

### 2.2 成功指标（V1）

| 类型 | 指标 | 目标 |
|------|------|------|
| 习惯 | 孩子 7 日任务完成率 | ≥ 70% |
| 习惯 | 连续打卡 ≥ 7 天的孩子占比 | ≥ 50% |
| 留存 | 家庭次周留存 | ≥ 60% |
| 使用 | 家长每日打开并处理任务的比例 | ≥ 80% |
| 体验 | 完成一次打卡的操作步数 | ≤ 3 步 |
| 体验 | 首屏加载 | < 1.5s |

---

## 3. 用户角色与权限

| 角色 | 载体 | 权限 |
|------|------|------|
| **家长（主账号 / Admin）** | 微信登录（通常是爸爸或妈妈） | 创建家庭、管理孩子、建/改/停任务、打卡、发奖、看报表、设规则、邀请家人 |
| **家人（协同：爷爷奶奶 / 外公外婆等）** | 受邀加入同一家庭 | 打卡、查看孩子任务与进度（默认不可改规则，可由主账号授权） |
| **孩子（展示视图）** | 家长手机（必要时家庭平板） | 查看今日任务、看积分、兑换、看成就/宠物（不可改规则） |

> 目标用户为**孩子的家人**（父母、爷爷奶奶、外公外婆等）。MVP 支持「1 个主账号 + 多家人 + 多孩子」；家人权限细分（能否改规则 / 发奖）在 V1 完善。

---

## 4. 功能需求（FR）

> 优先级：P0 = MVP 必须；P1 = V1 应有；P2 = 二期/后续。
> 每条给出「验收标准」，便于开发与验收对齐。

### 4.1 账号与家庭（FR-100）

| ID | 功能 | 优先级 | 验收标准 |
|----|------|--------|---------|
| FR-101 | 微信一键登录 | P0 | 授权后自动建/取用户，无需密码 |
| FR-102 | 创建家庭 | P0 | 首次登录引导创建家庭，生成家庭名 |
| FR-103 | 添加孩子档案 | P0 | 可添加 ≥1 个孩子，含昵称/头像/生日/年级（默认一年级） |
| FR-104 | 邀请家人加入 | P1 | 生成邀请码，家人（爷爷奶奶、外公外婆等）扫码加入同一家庭 |
| FR-105 | 孩子视角切换 | P0 | 家长可一键切到「孩子视图」，无需重新登录 |

### 4.2 每日任务与模板（FR-200）

| ID | 功能 | 优先级 | 验收标准 |
|----|------|--------|---------|
| FR-201 | 任务模板库（内置） | P0 | 预置一年级常用任务（作业/跳绳/写字/阅读…），一键启用 |
| FR-202 | 创建自定义任务 | P0 | 可设名称、图标、分类、积分值、**每日截止时间**、频率 |
| FR-203 | 任务分类 | P0 | 至少支持：学习 / 运动 / 生活 / 习惯 / 阅读 |
| FR-204 | 任务分配给某孩子 | P0 | 可为不同孩子启用不同任务 |
| FR-205 | 每日任务实例化 | P0 | 系统按频率自动生成「当天任务清单」（见 §5.1） |
| FR-206 | 编辑 / 停用任务 | P0 | 停用后不影响历史记录 |
| FR-207 | 批量操作 | P1 | 一次为多孩子启用同一模板 |
| FR-208 | 挑战 / 一次性任务 | P2 | 限时挑战、单次任务 |

### 4.3 打卡与按时判定（FR-300）★核心

| ID | 功能 | 优先级 | 验收标准 |
|----|------|--------|---------|
| FR-301 | 一键打卡 | P0 | 在「今日任务」列表点一下即完成，≤3 步 |
| FR-302 | 按时判定 | P0 | 打卡时间 ≤ 截止时间 → 记为「按时」；否则「超时」 |
| FR-303 | 补录打卡 | P0 | 家长可补录过去时间并标注（避免漏记） |
| FR-304 | 照片/证据（可选） | P1 | 可附 1 张图（如写字作业），非必填 |
| FR-305 | 打卡撤销/修改 | P1 | 家长可撤销或修正当天打卡 |
| FR-306 | 计时器（可选） | P2 | 跳绳/阅读可用内置计时器辅助 |

### 4.4 审核（FR-400）—— 本版不启用

**决策（Q3）**：打卡统一在家长手机上完成，**无需审核、自动通过**。因此 MVP / V1 不做独立审核流程。

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| FR-401 | 自动通过 | P0 | 打卡即生效，即时发放积分，无「待审核」状态 |
| FR-402 | 打卡撤销 / 修正 | P1 | 家长可撤销或修正当天打卡（唯一「纠错」入口） |
| FR-403 | （未来）孩子自主打卡审核 | P2 | 若后续开放孩子自己打卡，再引入审核流 |

> 因「无审核」，原 PRD 的「待审批列表 / 批量审核 / 驳回原因」全部移除，流程与操作步数进一步简化。

### 4.5 积分系统（FR-500）★核心

| ID | 功能 | 优先级 | 验收标准 |
|----|------|--------|---------|
| FR-501 | 积分发放 | P0 | 打卡完成后自动按规则发放，实时更新余额 |
| FR-502 | 积分明细 | P0 | 记录每笔「+/-分」「原因」「时间」，孩子可见 |
| FR-503 | 积分余额 | P0 | 首页/孩子视图常驻显示当前积分 |
| FR-504 | 积分规则配置 | P1 | 可设每日上限、连续奖励、按时加成、周末倍数 |
| FR-505 | 手动调整 | P1 | 家长可手工加/减分并填原因 |
| FR-506 | 积分重置 | — | 决策：**不按期清零**，不开发；如需归零用 FR-505 手动调整 |

### 4.6 奖励兑换（FR-600）

| ID | 功能 | 优先级 | 验收标准 |
|----|------|--------|---------|
| FR-601 | 创建奖励 | P0 | 设名称、图标、所需积分、库存（可为无限） |
| FR-602 | 奖励分类 | P0 | 实物 / 特权 / 体验 / 零花钱 / 陪伴券 |
| FR-603 | 兑换 | P0 | 积分足够即可发起兑换，扣分并生成记录 |
| FR-604 | 兑换确认（可关） | P2 | 默认免审批直接扣减；家长可选开启二次确认 |
| FR-605 | 库存 / 限购 | P2 | 控制稀缺奖励 |

### 4.7 成长记录与统计（FR-700）

| ID | 功能 | 优先级 | 验收标准 |
|----|------|--------|---------|
| FR-701 | 今日概览 | P0 | 显示今日待完成 / 已完成 / 今日积分 |
| FR-702 | 打卡日历 | P1 | 月历显示每日完成情况（如每任务一个色块/圆点） |
| FR-703 | 连续打卡天数 | P1 | 显示当前连续天数与最长纪录 |
| FR-704 | 趋势图 | P1 | 近 7/30 天完成率、积分趋势 |
| FR-705 | 周报 | P2 | 每周生成习惯报告 |

### 4.8 提醒通知（FR-800）

| ID | 功能 | 优先级 | 验收标准 |
|----|------|--------|---------|
| FR-801 | 订阅消息授权 | P0 | 引导家长订阅任务提醒 |
| FR-802 | 每日任务提醒 | P0 | 可设时间推送「今日任务未完成」提醒 |
| FR-803 | 截止前提醒 | P1 | 任务临近截止提醒 |
| FR-804 | 积分/兑换通知 | P2 | 变动通知 |

### 4.9 游戏化（二期，FR-900）

| ID | 功能 | 优先级 |
|----|------|--------|
| FR-901 | 成就徽章（分类 + 隐藏） | P2 |
| FR-902 | 宠物养成（选择/喂食/进化） | P2 |
| FR-903 | 贴纸墙 / 装扮 | P2 |

---

## 5. 业务规则详述

### 5.1 按时判定规则

- 每个「每日任务」可配置 `due_time`（如 19:00）。
- **每日任务实例化**：系统在每天 0 点（或首次打开时）为该孩子生成当日任务实例 `task_instance{ date, task_id, child_id, due_time, status }`。
  - ✅ 该设计解决现有 `tasks` 表无法精确记录「每天是否按时」的问题。
- 打卡时比较 `submit_time` 与 `due_time`：
  - `submit_time ≤ due_time` → **按时**（on_time）
  - `submit_time > due_time` → **超时**（late）
- 宽限期 `grace_minutes`（默认 0，可配 10/30 分钟）：宽限内仍算按时。
- 未在当天完成 → 次日标记为 **漏做**（missed）。

### 5.2 积分计算引擎（MVP）

```
最终积分 = base × 倍数
         + 按时加成 on_time_bonus
         + 连续打卡奖励 streak_bonus
         − 越界修正（受每日上限约束）

base              ：任务基础积分（家长设定，默认 1–5）
倍数 multiplier   ：周末/生日倍数（默认 1，可配 2 / 3）
on_time_bonus     ：按时完成额外 +1（可配；超时则不得）
streak_bonus      ：连续天数达 7/14/30 天，额外 +10/+15/+70（见 5.3）
daily_limit       ：每日积分上限（默认 50，防止通胀）
```

> ⚠️ **去掉随机奖励**：现有 TECHNICAL_SPEC 含「10% 概率随机 +1~5 分」。对一年级孩子，随机性会破坏「做到就得到」的因果认知与可解释性，**MVP 不采用**（如需惊喜感，改用「隐藏成就」，见二期）。

### 5.3 连续打卡奖励

| 连续天数 | 额外奖励 |
|---------|---------|
| 7 天 | +10 |
| 14 天 | +15 |
| 30 天 | +70 |
| 中断 | 连续计数归零（保留历史最长纪录） |

- 连续计数按「孩子 × 任务分类」或「孩子整体」统计，MVP 建议先做**孩子整体**（简单可解释）。

### 5.4 每日上限与通胀控制

- 每日得分封顶（默认 50），避免「刷任务」快速膨胀导致奖励贬值。
- 奖励定价与积分产出需**成对调校**：目标为「孩子约 1–2 周可兑换一个中档奖励」，保持动力。
- 提供家长侧的「积分健康度提示」（产分过快/过慢时温和提醒，P2）。

### 5.5 积分生命周期

- **决策：积分不清零**，长期累计；仅在**兑换奖励时扣减**（写入积分流水，余额 = 累计获得 − 已兑换）。
- 不做按期重置；如个别家庭需归零，用「手动调整」（FR-505）人工处理。

---

## 6. 数据模型（MVP 收敛版）

> 相对 TECHNICAL_SPEC 的 11 表，MVP 先落 **9 张核心表**，其余（宠物、成就、通知日志）后置。

```javascript
// 1) families 家庭
{ _id, name, invite_code, owner_uid, rules_config:{ max_daily_points, weekend_multiplier,
  on_time_bonus, grace_minutes }, created_at }

// 2) users 家长用户
{ _id, uid, openid?, nickname, avatar, family_id, role,
  subscription:{ task_remind:bool, remind_time }, created_at }
// 说明：uid = uni-id 用户 ID（登录态主键）；openid 仅作微信端冗余

// 3) children 孩子档案
{ _id, family_id, name, avatar, birthday, grade:"一年级", pet_id?,
  points_balance, streak_days, streak_best, created_at }

// 4) tasks 任务定义
{ _id, family_id, creator_id, title, icon, category, points_value,
  frequency:"daily|once", due_time:"19:00", grace_minutes, auto_approve,
  assign_to:[child_id], status:"active|inactive", created_at }

// 5) task_instances 每日任务实例   ← 新增，支撑按时判定与日历
{ _id, task_id, child_id, family_id, date:"2026-09-13",
  due_time, status:"todo|done|late|missed", submit_time, completion_id }

// 6) completions 打卡记录
{ _id, instance_id, task_id, child_id, family_id, date,
  submit_time, on_time:bool, proof_url?, remark,
  status:"approved", approver_id?, approve_time?, points_awarded }
// 说明：本版免审核，status 恒为 "approved"（预留 pending/rejected 供未来孩子自主打卡启用）

// 7) point_records 积分流水
{ _id, child_id, family_id, type:"earn|spend|adjust",
  amount, balance_before, balance_after, reason, related_id, created_at }

// 8) rewards 奖励
{ _id, family_id, title, icon, category, points_cost, stock, limit_per_person,
  status:"active|inactive", created_at }

// 9) redemptions 兑换记录
{ _id, reward_id, child_id, family_id, points_spent,
  status:"pending|done|cancelled", created_at, handled_at }
```

**建议索引**：`users.uid`（唯一）、`children.family_id`、`task_instances.{child_id,date}`、`point_records.{child_id,created_at}`、`rewards.family_id`。

**待办**：`task_instances` 的生成时机（uniCloud 定时触发器 vs 客户端懒生成）需在技术方案中确定，见 §7.5。

---

## 7. 技术方案

### 7.1 架构选型（三方案对比）

| 维度 | 方案 A：微信原生 + 微信云开发 | 方案 B：uni-app + **uniCloud** | 方案 C：uni-app/原生 + 自建后端 |
|------|------------------------|------------------------|------------------------------|
| 前端 | WXML/WXSS/JS | Vue3 + uni-app | Vue3 + uni-app |
| 后端 | 微信云开发（云函数/云数据库/云存储） | **uniCloud（云对象 + 云数据库 + 云存储）** | Node/FastAPI + MySQL + 自建服务器 |
| 上线速度 | ⭐⭐⭐⭐⭐ 最快 | ⭐⭐⭐⭐ | ⭐⭐ |
| 运维成本 | ⭐⭐⭐⭐⭐ 免运维 | ⭐⭐⭐⭐⭐ 免运维 | ⭐⭐ 需备案域名/服务器/运维 |
| 多端能力 | ⭐ 仅微信 | ⭐⭐⭐⭐⭐ **全端原生**（小程序/H5/App/鸿蒙） | ⭐⭐⭐⭐ |
| 数据自主 | ⭐⭐⭐ 云端托管 | ⭐⭐⭐ 云端托管（腾讯云/阿里云可选） | ⭐⭐⭐⭐⭐ |
| 开发量 | 最小 | 中 | 最大 |
| 适合 | **快速验证、仅微信单端** | **已规划多端** | 已有后端体系/数据自控 |

**✅ 已确定（Q1，2026-09-13 v1.2 修订）：选方案 B — uni-app (Vue 3) + uniCloud**

理由：
- 一套代码同时产出**微信小程序 + H5 + 手机 App**（iOS / Android），且**后端一套云对象全端通用**，真正满足「保留多端、为 App 预留」的目标；
- **为什么不用微信云开发**：微信云开发天然只服务微信小程序——H5 端仅有 Web SDK 且**仅未登录模式、无法获取登录态**；App 端需走「多端应用 + 环境共享」，需显式传 appid、开未登录权限并配安全规则，配置繁琐。结果是**前端能跨端、后端调用层却锁死在微信**，与多端目标相冲突；
- uniCloud 是 DCloud 在**同一套腾讯云 TCB 底层**上做的跨端统一封装：**腾讯云版价格与微信云开发相同**，另有更便宜的阿里云版可选；自带 clientDB、uni-id 账号体系、DB Schema 声明式权限、云对象等能力，并有官方 wx2unicloud 迁移路径；
- 免运维、上线快，仍符合第一版「快速可用」的目标。

> 说明：方案 A（微信原生）因仅支持微信、不利于多端扩展，未采用；方案 C（自建后端）暂不采用，若未来需要更强的数据自主 / 私有化部署，再评估迁移。多端发布优先级建议：**微信小程序 → H5 → App**。

### 7.2 关键技术点

- **登录**：小程序端 `uni.login` 取 code → **uni-id-co 的 `loginByWeixin`** 换取登录态与用户身份；H5 / App 端可平滑切换为手机号、一键登录或第三方登录（**同一套 uni-id 账号体系**，无需重构）。
- **多端一致性**：云对象、云数据库、云存储**全端通用**；前端统一用 `uniCloud.importObject('xxx')` 调用，无需按端改写调用层。
- **数据权限**：用 **DB Schema（`.schema.json`）声明式配置**读写权限与字段校验，权限与业务代码解耦（替代微信云开发中按 openid 的隐式权限）。
- **订阅消息**：任务提醒依赖微信订阅消息，需在用户点击时申请 `tmplIds` 授权。
- **打卡即时反馈**：前端本地先出动画/音效，再落库，保证「即时感」。

### 7.3 UI 方向（面向一年级）

- 主色温暖（橙 #FF9500 + 蓝 #5AC8FA），大圆角、大图标、大按钮。
- 孩子视图：Emoji/插画 + 语音 + 动画为主，文字尽量 ≤6 字。
- 家长视图：信息密度可略高，但核心操作 ≤3 步。

### 7.4 页面与信息架构（MVP）

```
家长端（默认）
├── 首页        今日概览 + 待办 + 快捷打卡
├── 任务        任务列表 / 模板库 / 创建
├── 奖励        奖励管理 / 兑换扣减
├── 报表        日历 + 连续天数 + 趋势
└── 我的        孩子管理 / 家人管理 / 家庭设置 / 规则

孩子视图（同一小程序内切换）
├── 今日任务    大卡片列表 + 一键完成 + 积分动画
├── 我的积分    余额 + 明细 + 可兑换
└── 我的        连续天数 / 徽章（二期宠物）
```

### 7.5 待技术验证项

1. `task_instances` 生成策略（uniCloud 定时触发器 vs 懒生成）。
2. 订阅消息模板申请与频次限制。
3. 每日上限与连续奖励的原子性（并发扣加分的幂等）。
4. 服务商选择（腾讯云 / 阿里云）与服务空间的免费额度是否够用。

---

## 8. 非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | 首屏 < 1.5s；普通操作响应 < 500ms |
| 可用性 | 核心打卡支持弱网重试；失败提示明确 |
| 兼容 | iOS / Android 微信最新及前 2 个主流版本 |
| 易用 | 新家长 3 分钟内完成「建家庭 → 建任务 → 打卡」首次闭环 |
| 可访问 | 大字号、对比度达标，适配儿童视觉 |
| 安全 | 家庭数据隔离；积分变动记录不可篡改（流水留痕） |
| 数据 | 每日备份；支持导出（后期） |

---

## 9. 合规与儿童隐私

- 遵守《儿童个人信息网络保护规定》《个人信息保护法》《网络安全法》。
- **最小化采集**：不采集人脸、定位、声纹、通讯录；孩子昵称可匿名化。
- 14 岁以下需**监护人同意**（由家长账号操作的声明与授权流程承载）。
- 小程序类目/资质、内容安全（无 UGC 公开内容，风险低）。
- 明确「非医疗/非教育评价」定位，避免诱导沉迷与不当激励。

---

## 10. 里程碑与排期（里程碑制，MVP 优先）

| 里程碑 | 目标 | 主要交付 | 周期（参考） |
|--------|------|---------|------------|
| **M0 需求定稿** | 本文评审通过、决策项确认 | REQUIREMENTS_SPEC + 决策记录 | 本轮 |
| **M1 MVP 可用** | 跑通「任务 → 打卡 → 积分 → 兑换」闭环 | 家庭/孩子、任务与模板、打卡+按时、积分、简单兑换、今日视图、孩子展示视图 | 2–3 周 |
| **M2 习惯强化** | 让「坚持」看得见 | 打卡日历、连续天数、趋势图、订阅消息提醒 | 2 周 |
| **M3 游戏化** | 提升长期吸引力 | 成就徽章、宠物养成、贴纸/装扮 | 2–3 周 |
| **M4 打磨上线** | 稳定 + 合规 | 体验优化、性能、合规检查、小程序审核发布 | 1–2 周 |

> 合计约 **7–10 周**（视投入人力）。**每个里程碑都可独立使用**，M1 后即已具备产品核心价值。

---

## 11. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 家长自己坚持不下去（产品被弃用） | 高 | 极简操作 + 提醒 + 家长端效率优先 |
| 多个家人协作、规则不一致 | 中 | 明确「主账号改规则」；家人默认只打卡/查看 |
| 积分通胀导致奖励贬值 | 中 | 每日封顶 + 产出/定价配对 + 健康度提示 |
| 孩子新鲜感衰减 | 中 | M2/M3 逐步引入连续奖励、成就、宠物 |
| 「按时」执行难（孩子拖） | 中 | 截止前提醒 + 家长可视化进度，不靠惩罚 |
| 孩子无法独立使用 | — | 已按「家长端 + 孩子展示视图」设计，非风险 |
| 微信审核/订阅消息限制 | 中 | 提前申请类目与模板，准备降级（站内提醒） |
| 数据安全/隐私 | 高 | 最小化采集 + 数据隔离 + 合规声明 |

---

## 12. 附录 A：一年级每日任务模板库（内置建议）

| 分类 | 任务 | 建议基础分 | 默认截止 | 说明 |
|------|------|-----------|---------|------|
| 学习 | 按时完成作业 | 3 | 19:00 | 核心任务 |
| 学习 | 写字练习 1 页 | 2 | 20:00 | 可附照片 |
| 阅读 | 亲子/自主阅读 20 分钟 | 2 | 20:30 | 可配计时器 |
| 运动 | 跳绳 100 个 | 2 | 18:30 | 可配计时器 |
| 生活 | 自己整理书包 | 1 | 20:00 | |
| 生活 | 早晚刷牙 | 1 | 21:00 | 每日 2 次可拆 |
| 习惯 | 9 点前上床睡觉 | 2 | 21:00 | 家长确认 |
| 习惯 | 主动阅读打卡（额外） | 1 | — | 增量加分，鼓励超出要求 |

> 借鉴 kids-points-v2 的「增量贡献」理念：**份内事给基础分，超出要求的行为给额外分**，避免一切靠积分驱动。

---

## 13. 决策记录

| # | 决策项 | 结论 | 状态 |
|---|--------|------|------|
| Q1 | 技术架构 | **uni-app (Vue 3) + uniCloud**，保留多端 / App 扩展 | ✅ 已确认（v1.2 修订） |
| Q2 | 孩子端形态 | **家长手机上的展示视图**（无孩子独立账号） | ✅ 已确认 |
| Q3 | 打卡与审核 | **家长手机打卡，无需审核、自动通过** | ✅ 已确认 |
| Q4 | 积分周期 | **不清零**，兑换后扣减 | ✅ 已确认 |
| Q7 | 目标用户 | **孩子的家人**（父母、爷爷奶奶、外公外婆等） | ✅ 已确认 |
| Q5 | 「按时」宽限期 | 暂定默认 **10 分钟**，可后续调整 | ⏳ 待定（已给默认值） |
| Q6 | 首个里程碑范围 | 建议 **先做 M1** 跑通闭环 | ⏳ 待定（已给建议） |

> 剩余待定项不阻塞开发，可按默认值先行推进 M1。

---

**文档结束**

*本文为需求基线，确认后可作为开发拆分与验收依据。*
